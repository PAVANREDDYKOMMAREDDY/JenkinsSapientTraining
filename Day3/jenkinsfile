pipeline {
    agent any

    stages {
        stage ('Compile Stage') {

            steps {

		
          	dir('Day3'){
            		sh "$PWD"
         	}
          
                withMaven(maven : 'Maven 3.6.3') {
		    
                    sh 'mvn clean install'
                }
            }
        }

        


        
    }
}