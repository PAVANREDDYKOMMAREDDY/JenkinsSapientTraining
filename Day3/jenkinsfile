pipeline {
    agent any

    stages {
        stage ('Compile Stage') {

            steps {
                withMaven(maven : 'Maven 3.6.3') {
		    sh 'cd Day3'
                    sh 'mvn clean install'
                }
            }
        }

        


        
    }
}